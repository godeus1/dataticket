[build]
builder            = "DOCKERFILE"
dockerfilePath     = "Dockerfile"

[deploy]
# Porta exposta pelo Puma / Thruster
# Railway injeta $PORT automaticamente
startCommand       = "bin/start"
healthcheckPath    = "/up"
healthcheckTimeout = 300

restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 5
