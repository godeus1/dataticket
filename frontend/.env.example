# URL da API Rails (Railway)
# Em dev: deixar vazio — o proxy do Vite redireciona /api → localhost:3001
# Em produção: preencher com a URL do Railway
VITE_API_URL=https://web-production-03f8b.up.railway.app/api/v1
