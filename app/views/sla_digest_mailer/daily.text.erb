Resumo SLA — <%= Date.current.strftime('%d/%m/%Y') %>
Olá, <%= @user.first_name %>.

<% if @expired.any? %>
🔴 TICKETS COM SLA VENCIDO (<%= @expired.count %>)
<% @expired.each do |tk| %>
  • <%= tk.id %> | <%= tk.title %> | Prazo: <%= tk.deadline&.strftime('%d/%m %H:%M') %> | <%= tk.status %>
<% end %>
<% end %>

<% if @expiring_today.any? %>
🟡 VENCEM HOJE (<%= @expiring_today.count %>)
<% @expiring_today.each do |tk| %>
  • <%= tk.id %> | <%= tk.title %> | Prazo: <%= tk.deadline&.strftime('%d/%m %H:%M') %> | <%= tk.status %>
<% end %>
<% end %>

Ver todos os tickets: <%= @url %>

---
Este e-mail foi enviado automaticamente pelo DataTicket.
